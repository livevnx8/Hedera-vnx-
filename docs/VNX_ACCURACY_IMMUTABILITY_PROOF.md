# VNX Accuracy Report — Immutability Proof

**Status:** ✅ **VERIFIED — This report has NOT been edited.**

| Field | Value |
|-------|-------|
| **Network** | Hedera Mainnet |
| **Topic ID** | `0.0.10416185` |
| **Sequence Number** | `2790` |
| **Consensus Timestamp** | `1778810840.687010077` |
| **Payer Account** | `0.0.10294360` |
| **Verification Date** | 2026-05-15 |

---

## What Was Verified

This document proves that the accuracy report stored at HCS mainnet sequence **2790** is **immutable** — it was written once by the Hedera consensus nodes and has not been changed since.

### Verification Steps

1. **Fetch from mirror node** — Query `mainnet-public.mirrornode.hedera.com` for sequence 2790
2. **Decode payload** — Base64-decode the HCS message body
3. **Recompute hash** — Reconstruct the original Python payload and compute SHA-256
4. **Compare** — The recomputed hash matches the `proof_hash` stored in the message
5. **Cross-check** — Metrics are cross-referenced against the local SQLite prediction DB

---

## Hash Verification

```
Stored proof_hash:     ea4f6fee3612f03ed9d0d8eaedc8359f4bd58b52e275ab658319ec7de9484980
Recomputed hash:       ea4f6fee3612f03ed9d0d8eaedc8359f4bd58b52e275ab658319ec7de9484980
Status:                ✅ MATCH
```

The hash was recomputed by:
1. Extracting the `metadata` block from the decoded HCS message
2. Reconstructing the original Python payload (restoring float values that the TypeScript bridge converted to integers)
3. Serializing with `json.dumps(dict, sort_keys=True)` — the same method used at emission time
4. Computing `SHA-256` of the resulting JSON string

---

## On-Chain Payload (Decoded)

```json
{
  "iso_time": "2026-05-15T02:07:20Z",
  "metrics": {
    "down_accuracy_pct": 50,
    "high_conf_accuracy_pct": 46.2,
    "last_10_accuracy_pct": 70,
    "last_50_accuracy_pct": 56,
    "low_conf_accuracy_pct": 56.1,
    "overall_accuracy_pct": 53.7,
    "total_scored": 54,
    "up_accuracy_pct": 57.7
  },
  "model": "BitLattice-ONNX-v3.1",
  "report_type": "swarm.accuracy",
  "sample_size": {"last_10": 10, "last_50": 50, "total": 54},
  "source": "vnx_prediction_swarm",
  "timestamp": 1778810840.3375561,
  "token": "HBAR/USD",
  "version": "3.1"
}
```

---

## Independent Verification

Anyone can verify this independently. No credentials required.

### Via Python Script

```bash
source .env && python3 scripts/verify_accuracy_immutability.py
```

### Via curl (API Endpoint)

```bash
curl http://localhost:8080/proof/verify-accuracy/2790
```

### Via curl (Direct Mirror Node)

```bash
curl https://mainnet-public.mirrornode.hedera.com/api/v1/topics/0.0.10416185/messages/2790
```

### Via HashScan

- [HashScan Transaction](https://hashscan.io/mainnet/transaction/0.0.10294360@1778810837.153527342)
- [HashScan Topic View](https://hashscan.io/mainnet/topic/0.0.10416185?seq=2790)

---

## Why This Proves Immutability

1. **Hedera Consensus Service** — Every message gets an immutable consensus timestamp assigned by the hashgraph algorithm. No one — not the operator, not the mirror node, not VNX — can change a message after it has been assigned a timestamp.

2. **SHA-256 proof hash** — The `proof_hash` field inside the message is the cryptographic fingerprint of the original payload. If even one character in the payload were changed, the hash would not match.

3. **Independent verification** — The mirror node REST API is open-source (Apache-2.0) and requires no authentication. Anyone can query it from anywhere in the world.

4. **Local DB cross-check** — The accuracy numbers in the on-chain message are cross-referenced against the independent SQLite database. While the DB continues to grow (new predictions are scored), the on-chain snapshot is frozen.

---

## Conclusion

The accuracy report at mainnet sequence **2790** is **cryptographically verified as unedited**.

- ✅ Hash matches original payload
- ✅ Consensus timestamp is permanent
- ✅ Independent mirror node confirms the data
- ✅ Anyone can reproduce this verification

---

*Generated by VNX Immutability Verifier*  
*Verified via Hiero Mirror Node REST API (Apache-2.0)*  
*Hedera Mainnet — Topic 0.0.10416185 — Sequence 2790*
