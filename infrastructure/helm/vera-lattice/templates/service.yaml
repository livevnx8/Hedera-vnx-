apiVersion: v1
kind: Service
metadata:
  name: {{ include "vera-lattice.fullname" . }}
  labels:
    {{- include "vera-lattice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vera-lattice.selectorLabels" . | nindent 4 }}
